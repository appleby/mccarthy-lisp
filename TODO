* Add testing base class that creates "standard objects".
* Wrap all tests in anon namespace.
* Write some tests in lisp.
* Better error messages for Read errors.
* Flush input on Read error.
* Cleanup TODOs
* Allow comments.
* Allow () for nil.
* Allow symbol as lambda-list to bind all args as a list.
* Accept filenames as command line args.
